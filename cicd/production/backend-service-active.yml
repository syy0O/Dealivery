kind: Service
apiVersion: v1
metadata:
  name: prod-backend-service-active
spec:
  type: NodePort
  selector:
    app: prod-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30101