kind: Service
apiVersion: v1
metadata:
  name: dev-frontend-service-active
spec:
  type: NodePort
  selector:
    app: dev-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30100