kind: Service
apiVersion: v1
metadata:
  name: backend-service-oauth-preview
spec:
  type: NodePort
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30104